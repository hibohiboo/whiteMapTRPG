//- コンパイル後に残らないコメント。 // でコメントするとコンパイル後に残る
doctype html
html(lang="ja")
  head
    meta(charset='UTF-8')
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    block title
    block meta
      //- 上書き可能
      //- コンテンツの種類
      meta(name='og:type', content="game") 
    include _meta_common
    //- 文字コードでFontAwesomeを指定できるようにする設定
    script  FontAwesomeConfig = { searchPseudoElements: true };
    //- Font Awesomeは初期表示に関わるので先に読み込む。
    script(defer src="https://use.fontawesome.com/releases/v5.0.2/js/all.js")
    //- 指定したフォントをダウンロード
    link(href="https://fonts.googleapis.com/css?family=Montserrat&text=WhiteMapTRPG" rel="stylesheet")
    link(href="../assets/css/styles.css" rel="stylesheet" )
    block css
    block headjs
  block body
  //- jsを最後に持ってくることによりページの表示を早める
  //- IE9以降に対応
  script(src="https://code.jquery.com/jquery-3.2.1.min.js")
  //- トグルボタンでナビゲーションメニューを開閉するスクリプト
  script $(()=>{$(".headC").click(()=>$(".headB").slideToggle("fast"))})
  block footjs